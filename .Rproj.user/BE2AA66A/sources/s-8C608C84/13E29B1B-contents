set_randText <- function(){
  require(jsonlite)
  random_text <- c('Working',
                   'Keeping good work',
                   'Dismantling data',
                   'Purifying')
  
  jsonlite::write_json(random_text, path = 'meta/randText.json')
}
